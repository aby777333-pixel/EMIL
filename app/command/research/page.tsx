import AdminClient from '@/app/admin/_components/admin-client'

export const dynamic = 'force-dynamic'

// Research Ops — the research-governance console (sources, hypotheses,
// contradictions, confidence overrides, strategy rollback, audit trail).
export default function CommandResearchPage() {
  return <AdminClient />
}
